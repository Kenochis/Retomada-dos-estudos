// mostre cada elemento usando for...of.

const frutas = ["Maçã", "Banana", "Uva"];

for (const fruta of frutas) { // Tudo que estiver no array vai passar de 1 em 1 na variável fruta.
    console.log(fruta);
};

// Use for...of quando só precisar do valor